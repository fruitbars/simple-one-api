version: '3.8'

services:
  simple-one-api:
    image: fruitbars/simple-one-api
    container_name: simple-one-api
    ports:
      - "9090:9090"
    volumes:
      - /Users/admin/GolandProjects/myopengoprjs/simple-one-api/myconfigs/:/app/
    restart: unless-stopped